#include "Base.xcconfig"

HOOPS_APP_ENV = production
// Public GA cannot ship as an on-device video product. Production Release
// builds must receive production cloud analysis/edit URLs from LocalSecrets or CI.
HOOPS_CLOUD_LAUNCH_MODE = enabled
