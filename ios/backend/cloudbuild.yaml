substitutions:
  _SERVICE_NAME: hoops-ai-api
  _REGION: us-central1
  _ARTIFACT_REPO: hoops-ai
  _ENVIRONMENT: production
  _GCS_BUCKET: charlie-hoops-ai-analysis-temp
  _JOBS_COLLECTION: analysisJobs
  _USAGE_COLLECTION: usageCounters
  _TASKS_QUEUE: analysis-jobs
  _MAX_RETURNED_CLIPS: "60"
  _TEAM_QUICK_SCAN_ENABLED: "true"
  _TEAM_QUICK_SCAN_CLIP_FRAMES_PER_CLIP: "6"
  _TEAM_QUICK_SCAN_RICH_CANDIDATE_CLIPS: "60"
  _TEAM_QUICK_SCAN_MAX_TOTAL_CLIP_FRAMES: "720"
  _TEAM_QUICK_SCAN_MAX_CANDIDATE_CLIPS: "160"
  _TEAM_QUICK_SCAN_MAX_OUTPUT_TOKENS: "6000"

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_ARTIFACT_REPO}/${_SERVICE_NAME}:$COMMIT_SHA
      - .

  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_ARTIFACT_REPO}/${_SERVICE_NAME}:$COMMIT_SHA

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - ${_SERVICE_NAME}
      - --image
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_ARTIFACT_REPO}/${_SERVICE_NAME}:$COMMIT_SHA
      - --region
      - ${_REGION}
      - --platform
      - managed
      - --port
      - "8080"
      - --allow-unauthenticated
      - --service-account
      - hoops-ai-api@$PROJECT_ID.iam.gserviceaccount.com
      - --set-env-vars
      - >-
        HOOPS_ENVIRONMENT=${_ENVIRONMENT},
        HOOPS_GCP_PROJECT_ID=$PROJECT_ID,
        HOOPS_GCP_REGION=${_REGION},
        HOOPS_GCS_BUCKET=${_GCS_BUCKET},
        HOOPS_FIRESTORE_JOBS_COLLECTION=${_JOBS_COLLECTION},
        HOOPS_FIRESTORE_USAGE_COLLECTION=${_USAGE_COLLECTION},
        HOOPS_CLOUD_TASKS_QUEUE=${_TASKS_QUEUE},
        HOOPS_CLOUD_RUN_BASE_URL=https://hoops-ai-api-$PROJECT_ID.${_REGION}.run.app,
        HOOPS_PUBLIC_API_ENABLED=false,
        HOOPS_ENABLE_LOCAL_UPLOAD_EMULATION=false,
        HOOPS_MAX_RETURNED_CLIPS=${_MAX_RETURNED_CLIPS},
        HOOPS_TEAM_QUICK_SCAN_ENABLED=${_TEAM_QUICK_SCAN_ENABLED},
        HOOPS_TEAM_QUICK_SCAN_CLIP_FRAMES_PER_CLIP=${_TEAM_QUICK_SCAN_CLIP_FRAMES_PER_CLIP},
        HOOPS_TEAM_QUICK_SCAN_RICH_CANDIDATE_CLIPS=${_TEAM_QUICK_SCAN_RICH_CANDIDATE_CLIPS},
        HOOPS_TEAM_QUICK_SCAN_MAX_TOTAL_CLIP_FRAMES=${_TEAM_QUICK_SCAN_MAX_TOTAL_CLIP_FRAMES},
        HOOPS_TEAM_QUICK_SCAN_MAX_CANDIDATE_CLIPS=${_TEAM_QUICK_SCAN_MAX_CANDIDATE_CLIPS},
        HOOPS_TEAM_QUICK_SCAN_MAX_OUTPUT_TOKENS=${_TEAM_QUICK_SCAN_MAX_OUTPUT_TOKENS}
      - --set-secrets
      - HOOPS_INTERNAL_PROCESS_SECRET=HOOPS_INTERNAL_PROCESS_SECRET:latest,HOOPS_OPENAI_API_KEY=HOOPS_OPENAI_API_KEY:latest

images:
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_ARTIFACT_REPO}/${_SERVICE_NAME}:$COMMIT_SHA
